# Cloudflare References

Do not paste whole docs here unless a prototype needs an offline fixture. Keep source URLs in `../../docs/cloudflare-references.md` and cite exact docs/blog pages in run receipts.

Important current theme: Cloudflare has enough primitives for this spine — Sandbox, Artifacts, Durable Objects, D1, R2, outbound Workers, Dynamic Workflows — but our prototype must validate the seams before we platform-ize.
