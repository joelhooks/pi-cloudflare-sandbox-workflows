# Production Code

Production-intended code goes here.

Do not import from `../prototypes`. Rewrite from captured learning.

First production modules expected later:

- `capsule/` — context capsule identity and metadata
- `context-packs/` — pack resolution and pinning
- `secrets/` — secret refs, leases, and auth materialization boundary
- `scheduler/` — concurrency leases and run queue
- `sandboxes/` — Cloudflare Sandbox lifecycle adapters
- `artifacts/` — Artifacts repo/ref adapters
- `wzrrd/` — review page publication metadata
